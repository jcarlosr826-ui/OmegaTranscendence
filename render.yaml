services:
  - type: web
    name: omega-backend
    runtime: node
    plan: free
    region: oregon
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production

  - type: static_site
    name: omega-frontend
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: build
    envVars:
      - key: REACT_APP_API_BASE_URL
        value: https://omega-backend-i15y.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
